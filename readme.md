**How to add access token:**

In the folder:
<pre>core/</pre>

create a file called:
<pre>getToken.js</pre>

with content:
<pre>
module.exports = {
    getToken: function() {
        return 'ENTER_ACCESS_TOKEN_HERE';
    }
};
</pre>

